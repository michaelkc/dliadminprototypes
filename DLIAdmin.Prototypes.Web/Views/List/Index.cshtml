@model IEnumerable<DLIAdmin.Prototypes.Web.Models.Bruger>

@{
    ViewBag.Title = "title";
}

<h2>title</h2>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ADLogonname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ADGuid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ADLogonname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ADGuid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
